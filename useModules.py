# Importing modules to be used in this file

import modules

modules.iAmAModule() # < the parenthesis must be used to call the function.
modules.iAmBaboon
modules.baboonLoop

# Note that the modules is the file name. The functionality is accessed with dot operator.
modules.baboonLoop() # this will work because it is being accessed from the module and called correctly.
baboonLoop() # this will throw a name error because it is not being accessed from the module.
